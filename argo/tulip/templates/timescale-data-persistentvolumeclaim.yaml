apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: timescale-data
  name: timescale-data
  namespace: tulip
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    limits:
      storage: 100G
    requests:
      storage: 100M
