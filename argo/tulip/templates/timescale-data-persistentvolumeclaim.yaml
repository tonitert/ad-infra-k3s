apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: timescale-data
  name: timescale-data
  namespace: tulip
spec:
  storageClassName: longhorn-expand
  accessModes:
    - ReadWriteOnce
  resources:
    limits:
      storage: 40G
    requests:
      storage: 35G
