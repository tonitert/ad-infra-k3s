apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: traffic
  namespace: tulip
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 40Gi
