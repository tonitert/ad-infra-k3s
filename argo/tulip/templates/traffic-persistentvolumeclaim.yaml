apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: traffic
  namespace: tulip
spec:
  storageClassName: longhorn-expand
  accessModes:
    - ReadWriteMany
  resources:
    limits:
      storage: 40G
    requests:
      storage: 20G
