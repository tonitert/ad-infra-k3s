apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rsync-keys
  namespace: tulip
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    limits:
      storage: 20M
    requests:
      storage: 10M
