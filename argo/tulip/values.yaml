enowars:
  teamId: 15
  teamAddress: "10.1.15.1"
  tickStart: "2026-07-18T12:00:00Z"
  tickLengthMs: 60000
  flagRegex: "ENO[A-Za-z0-9+/=]{48}"
  # ENOWARS attack.json contains the authoritative set of currently valid
  # attack info. The scraper replaces its cache on every round.
  flagLifetime: -1
  flagIdParser: "enowars"
  attackInfoEndpoint: "https://10.enowars.com/scoreboard/attack.json"
  # Add released service ports here when they are announced. Tulip still
  # captures and displays unlisted traffic through the "other" entry.
  services: []
  pcapRsyncSource: "root@10.1.15.1:/pcaps/"
